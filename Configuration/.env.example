# ============================================================================ 
# .env.example for security-assessor
# ============================================================================ 

# Google Gemini API Configuration
GEMINI_API_KEY=YOUR_GEMINI_API_KEY_HERE

# Flask Configuration
FLASK_ENV=development
DEBUG=true
PORT=8080

# Cache Configuration - LOCAL (Fallback)
CACHE_DIR=./Runtime/assessor_cache
CACHE_TTL_DAYS=7

# Cache Configuration - GOOGLE CLOUD STORAGE
# Set USE_GCS_CACHE=true to enable GCS storage
USE_GCS_CACHE=true
CACHE_BUCKET_NAME=YOUR_GCS_BUCKET_NAME_HERE

# Logging Configuration
LOG_LEVEL=INFO
LOG_FILE=./logs/assessor.log

# ============================================================================ 
# NOTES:
# ============================================================================ 
# GCS Cache:
# - When USE_GCS_CACHE=true, all cache files are stored in Google Cloud Storage
# - The bucket specified in CACHE_BUCKET_NAME must exist and be accessible
# - Make sure you have google-cloud-storage package installed
# - Authentication is handled by Google Cloud SDK (gcloud auth application-default login)
#
# Local Cache:
# - When USE_GCS_CACHE=false (or not set), cache uses local filesystem
# - Cache directory specified in CACHE_DIR will be created automatically
# - Good for development and testing without cloud dependencies
#
# Both modes:
# - Cache files are automatically timestamped
# - Stale cache entries (older than CACHE_TTL_DAYS) are refreshed automatically
# - Cache can be cleared via API or CLI commands